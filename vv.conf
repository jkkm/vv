basedir: ~/src
remote: origin
jobs: 4

exclude:
  - vv

# Repos outside basedir to include explicitly (full paths).
# include:
#   - ~/work/special-repo

# Per-tree overrides. Any key valid at the top level may be set here.
# trees:
#   linux:
#     remote: upstream
