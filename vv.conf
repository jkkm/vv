jobs: 4

# When a repo is skipped because a working branch is checked out,
# fast-forward its local default branch (e.g. main) in the background.
# Never forced; can be overridden per tree.
# ff_default_branch: true

basedirs:
  ~/src:
    exclude:
      - vv
    # trees:
    #   linux:
    #     remotes:
    #       - upstream
    #       - origin
    #   myproject:
    #     type: hg
    #     updatecmd: "make update"
    #   Shipwright:
    #     submodules: false
    #   pinned-checkout:
    #     ff_default_branch: false

# Additional basedirs:
# ~/work:
#   trees:
#     project-x:
#       updatecmd: "make update"

# Standalone repos not under any basedir.
# Each key is a path; value is tree config (or null for defaults).
# include:
#   ~/other/repo:
#     type: git
#     updatecmd: "custom cmd"
#   ~/simple/repo:
