basedir: ~/src
jobs: 4

exclude:
  - vv

# Repos outside basedir to include explicitly (full paths).
# include:
#   - ~/work/special-repo

# Per-tree overrides.
# trees:
#   linux:
#     remotes:
#       - upstream
#       - origin
#   ~/linux:
#     updatecmd: "git fetch linus && git merge linus/master"
#   myproject:
#     type: hg     # override autodetection; values: git, hg, svn, cvs
